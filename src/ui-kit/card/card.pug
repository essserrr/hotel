mixin card(props)
    - const { title, type = 'default' } = props;

    div(class=`ui-card ui-card--${type} ui-card__row`)
        if (title)
            .ui-card__column100.ui-card--margin-bottom-md
                h1 #{title}
        .ui-card__column100
            block
